{
  "name": "subgraph",
  "version": "0.0.2",
  "description": "DavinciDAO delegation tracking with unique delegator support",
  "main": "index.js",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build --skip-migrations",
    "deploy": "graph deploy davincidao-test-3 --skip-migrations",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.98.1",
    "@graphprotocol/graph-ts": "^0.38.2",
    "assemblyscript": "^0.28.9"
  }
}
